# Example env file — copy to .env.local and fill the values (DO NOT commit real secrets)

# Public Mapbox token for client-side use (starts with pk.)
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=pk.your_public_token_here

# MongoDB connection string for server/API
MONGODB_URI=mongodb+srv://username:password@cluster0.mongodb.net/dbname?retryWrites=true&w=majority

# Optional
NODE_ENV=development
